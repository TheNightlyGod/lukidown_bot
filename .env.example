API_ID=YOUR_API_ID
API_HASH=YOUR_API_HASH
BOT_TOKEN=YOUR_BOT_TOKEN

# RU_PROXY=socks5://user:password@ru-proxy-ip:port  # Proxy specifically for Russian platforms (Rutube, VK, etc.)
# SOCKS5_PROXY=socks5://user:password@ip:port        # General proxy (fallback for all or foreign traffic)

DOWNLOAD_DIR=downloads
MAX_FILE_SIZE_MB=2000

DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/lukidown
REDIS_URL=redis://redis:6379/0

DOWNLOAD_WORKERS=2
CANCEL_TTL_SECONDS=60
HISTORY_LIMIT=10

# REDDIT_COOKIE=if_you_need_this_uncomment